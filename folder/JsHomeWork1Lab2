let e
let x = +prompt('vvod')
let y = +prompt('vvod2')
let c = +prompt('vvod3')
let d = +prompt('vvod4')
let ElMax = Math.Pow(x, 3)
let ElMin = x
if (x * y > 3)
    if (ElMax < Math.log(Math.Pow(x * y, c * d)))
        e = Elmax = Math.log(Math.Pow(x * y, c * d))
if (0 <= x * y && x * y <= 3)
    if (e = 3 * ElMin > y)
        ElMin = y
if (3 * ElMin > ElMax < c * x)
    e = 3 * ElMin == Elmax == c * x
if (3 * ElMin > ElMax < d * y)
    e = 3 * ElMin == ElMax == d * y
if (x * y < 0)
    e = Math.Pow((2, c * d) - x)
